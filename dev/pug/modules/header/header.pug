header.header
		
	.header__top.container
		a(href="/" title='Родители РФ').header__logo.logo
			picture
				source(srcset='web/images/general/logo.svg' media="(min-width: 600px)")
				img(src='web/images/general/logo-small.svg' alt='Родители РФ')

		.header__location.location
			span Консультационный центр
			span МБДОУ «ЦРР – детский сад №138»
		
		a(href="tel:+74732647750").header__phone.info-icons
			+icon('phone')
			| +7 (473) 264-77-50

		.header__socials.socials
			+socials(socialsList)

		.header__buttons
			.header__icon
				button
					+icon('baby')
					| Личный кабинет
			
			.header__icon
				button
					+icon('eye')
					| Версия для слабовидящих
		form.search
			.search__item
				input(type="text" placeholder='Поиск по каталогу')
				button
					+icon('search')
					
	.header__bottom.container
		nav.header__nav
			.header__menu
				.header__title-menu
					.hide-nav
						span.hide-nav__line.hide-nav__line--top
						span.hide-nav__line.hide-nav__line--middle
						span.hide-nav__line.hide-nav__line--bottom
					span.hide-nav__text Каталог

				.header__sub-menu
					each item in subMenu
						.header__sub-item
							a(href="#").header__sub-link=item.title
							.header__sub-list
								each links in item.list
									a(href="#")=links
			
			.header__menu.header__menu--nav
				.header__title-menu.header__title-menu--nav
					.hide-nav
						span.hide-nav__line.hide-nav__line--top
						span.hide-nav__line.hide-nav__line--middle
						span.hide-nav__line.hide-nav__line--bottom
					span.hide-nav__text Меню

				ul.header__list
					each key in navList
						li.header__item
							a(href="#").header__link #{key}

					li.header__item.header__item--mobile
						.socials
							+socials(socialsList)
					li.header__item.header__item--mobile
						.location
							span Консультационный центр
							span МБДОУ «ЦРР – детский сад №138»

